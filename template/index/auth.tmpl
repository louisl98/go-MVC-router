{{define "title"}}Go Web App{{end}}
{{define "head"}}{{end}}
{{define "content"}}
<div class="container">
	<div class="page-header">
		<h1>Hello, {{.username}}</h1>
	</div>
	<p>Click {{LINK "profile" "here"}} to view your posts and videos.</p>
	{{template "footer" .}}
</div>
{{end}}
{{define "foot"}}{{end}}